# Plugin Script Packaging

Guidance for designing and verifying Agent Plugin scripts that work as contained Node runtimes in Git/source installations and as standalone platform binaries in tagged releases.

Use it through `agent-plugins:plugin-script-packaging`. Use `plugin-creator` for final plugin validation, installation, packaging, and release approval.
