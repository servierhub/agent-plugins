# Plugin Script Packaging

Guidance for designing and verifying Agent Plugin scripts through progressive language-specific branches. TypeScript is the default; Rust and Python have conditional source-install and native-release guidance.

Use it through `agent-plugins:plugin-script-packaging`. Use `plugin-creator` for final plugin validation, installation, packaging, and release approval.
