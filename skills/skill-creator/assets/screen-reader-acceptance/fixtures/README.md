# Synthetic validator fixtures — not evidence

These records are fictional and exist only to test validation. They do not document a manual session, cannot satisfy a required combination, and must never be copied into an acceptance report.
