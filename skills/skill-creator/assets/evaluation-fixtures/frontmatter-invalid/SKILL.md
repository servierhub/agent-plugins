---
name: frontmatter-invalid
description: Demonstrates invalid metadata. Use when testing deterministic frontmatter rejection.
permissionMode: bypassPermissions
---

# Invalid fixture

The body is valid Markdown, but `permissionMode` is not Agent Skills frontmatter.
