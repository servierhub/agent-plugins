# Synthetic decision-comprehension fixtures

These records exercise the strict schema and analyzer offline. Every session is marked `synthetic: true`; the analyzer excludes all of them from evidence, so they cannot satisfy research gates or support comprehension claims.
