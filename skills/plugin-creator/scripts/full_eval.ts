import { createHash, randomUUID } from "node:crypto";
import { existsSync, mkdirSync, linkSync, readFileSync, readdirSync, renameSync, rmSync, statSync, unlinkSync, writeFileSync } from "node:fs";
import { basename, dirname, join, resolve } from "node:path";
import { isPathWithin } from "./path_containment.js";
import { spawnSync } from "node:child_process";
import { fileURLToPath } from "node:url";
import { validateAgentPluginSchema } from "./validate_agent_plugin_schema.js";
import { validate } from "./validate_goose_plugin.js";
import { sourceHash } from "./package_manifest.js";
import { verifyPlugin, type Status } from "./verify_plugin_gates.js";
import { discoverPluginComponents } from "./component_evidence.js";
import { ExecutionEventWriter, protectedArtifactRef, replayExecutionEvents } from "./execution_event_stream.js";

export interface FullEvalOptions { pluginPath:string; workspace?:string; componentReceipts?:string[]; integration?:string; archive?:string; testsStatus?:string; humanReview?:string; minPassRate?:number; minDelta?:number; dryRun?:boolean; resume?:boolean; cancel?:boolean; }
export type JobStatus="planned"|"running"|"succeeded"|"failed"|"blocked"|"cancelled"|"skipped";
export type JobEvent="start"|"succeed"|"fail"|"block"|"cancel"|"retry"|"resume"|"skip";
export interface FullEvalJob { id:string; phase:string; depends_on:string[]; input_hash:string; idempotency_key:string; status:JobStatus; attempts:number; detail:string; output_hashes:Record<string,string>; }
export interface FullEvalState { schema_version:"1.0"; command:"full-eval"; run_id:string; graph_hash:string; revision:number; status:string; configuration:PersistedConfiguration; verification?:unknown; jobs:FullEvalJob[]; }
interface PersistedConfiguration { pluginPath:string; workspace:string; componentReceipts:string[]; integration:string; archive:string; testsStatus?:string; humanReview?:string; minPassRate?:number; minDelta?:number; }
interface Phase { name:string; status:"planned"|"running"|"pass"|"fail"|"blocked"|"skipped"; detail:string; }
interface Context { root:string; workspace:string; integration:string; archive:string; name:string; skills:string[]; components:Array<{kind:"skill"|"agent"|"hook"|"mcp"|"integration";key:string;name:string;path:string;source_sha256:string;receipt:string;available:boolean;command:string}>; }
const HERE=dirname(fileURLToPath(import.meta.url));
const PHASES=["validation","planning","execution","grading","aggregation","review","improvement","verification"] as const;
const STATE_FILE="full-eval-state.json";
const EVENT_FILE="full-eval-events.jsonl";
const LOCK_FILE=STATE_FILE+".lock";
const q=(value:string)=>JSON.stringify(value);
function directories(path:string):string[]{return existsSync(path)?readdirSync(path,{withFileTypes:true}).filter(e=>e.isDirectory()).map(e=>e.name).sort():[];}
function json(value:unknown):string{return JSON.stringify(value,(_k,v)=>v&&typeof v==="object"&&!Array.isArray(v)?Object.fromEntries(Object.entries(v).sort(([a],[b])=>a.localeCompare(b))):v);}
function hash(value:unknown):string{return createHash("sha256").update(typeof value==="string"?value:json(value)).digest("hex");}
function fileHash(path:string):string{return existsSync(path)?hash(readFileSync(path)):"missing";}
function pluginHash(root:string,archive:string):string{try{return sourceHash(root,[archive]);}catch{return "invalid";}}
function packagePlugin(root:string,archive:string){return spawnSync(process.execPath,[join(HERE,"package_goose_plugin.js"),root,archive],{encoding:"utf8"});}
function requireText(path:string):string{return readFileSync(path,"utf8");}
function receiptName(path:string):string|null{try{return JSON.parse(requireText(path))?.name??null;}catch{return null;}}
function context(options:FullEvalOptions):Context{
 const root=resolve(options.pluginPath),evaluations=join(root,"evaluations"),workspace=resolve(options.workspace??join(evaluations,"plugin"));
 if(!isPathWithin(evaluations,workspace)||workspace===evaluations)throw new Error("full-eval workspace must be a subdirectory of the plugin evaluations directory");
 const integration=resolve(options.integration??join(workspace,"integration"));
 if(!isPathWithin(workspace,integration))throw new Error("full-eval integration must be inside the workspace");
 const manifest=(()=>{try{return JSON.parse(requireText(join(root,"plugin.json")));}catch{return null;}})(),name=manifest?.name??basename(root),archive=resolve(options.archive??join(workspace,name+".zip"));
 if(!isPathWithin(workspace,archive))throw new Error("full-eval archive must be inside the workspace");
 const discovered=discoverPluginComponents(root),skills=discovered.filter(component=>component.kind==="skill").map(component=>component.id),supplied=options.componentReceipts?.map(path=>resolve(path))??[],receiptByKey=new Map<string,string>();
 for(const path of supplied){try{const value=JSON.parse(requireText(path));if(value?.artifact&&value?.name)receiptByKey.set(value.artifact+":"+value.name,path);}catch{}}
 const skillCreatorCli=resolve(HERE,"../../../skill-creator/dist/scripts/cli.js");
 const components:Context["components"]=discovered.map(component=>{const defaultReceipt=join(workspace,"components",component.kind,component.id,"receipt.json"),legacyReceipt=component.kind==="skill"?join(workspace,"components",component.id,"receipt.json"):defaultReceipt,receipt=receiptByKey.get(component.key)??(existsSync(defaultReceipt)?defaultReceipt:legacyReceipt);const command=component.kind==="skill"?"node "+q(skillCreatorCli)+" full-eval "+q(component.path)+" --workspace "+q(join(workspace,"components",component.kind,component.id))+" --resume --format json":"obtain typed "+component.kind+" evidence from its specialist and write "+q(defaultReceipt);return{kind:component.kind,key:component.key,name:component.id,path:component.path,source_sha256:component.source_sha256,receipt,available:existsSync(receipt),command};});
 if(discovered.some(component=>component.kind!=="skill")){const integrationReceipt=receiptByKey.get("integration:"+name)??join(workspace,"integration","receipt.json");components.push({kind:"integration",key:"integration:"+name,name,path:root,source_sha256:pluginHash(root,archive),receipt:integrationReceipt,available:existsSync(integrationReceipt),command:"run plugin integration scenarios and write typed coverage/handoff evidence to "+q(integrationReceipt)});}
 return{root,workspace,integration,archive,name,skills,components};
}
function fingerprints(c:Context,o:FullEvalOptions):Record<string,string>{
 const artifact=pluginHash(c.root,c.archive),componentInputs=c.components.map(x=>({key:x.key,source:x.kind==="integration"?artifact:x.source_sha256,receipt:fileHash(x.receipt)}));
 const own:Record<string,unknown>={validation:{artifact},planning:{skills:c.skills,artifact},execution:componentInputs,grading:{benchmark:fileHash(join(c.integration,"benchmark.json")),artifact,minPassRate:o.minPassRate??0.8,minDelta:o.minDelta??0},aggregation:{testsStatus:o.testsStatus??"blocked",artifact},review:{review:fileHash(join(c.integration,"review.html")),humanReview:o.humanReview??"pending",artifact},improvement:{artifact},verification:{artifact,benchmark:fileHash(join(c.integration,"benchmark.json")),review:fileHash(join(c.integration,"review.html")),testsStatus:o.testsStatus??"blocked",humanReview:o.humanReview??"pending"}};
 const result:Record<string,string>={}; for(let i=0;i<PHASES.length;i++){const phase=PHASES[i],dependencies=i?[result[PHASES[i-1]]]:[];result[phase]=hash({phase,input:own[phase],dependencies});}return result;
}
function persistedConfiguration(c:Context,o:FullEvalOptions):PersistedConfiguration{return{pluginPath:c.root,workspace:c.workspace,componentReceipts:c.components.map(x=>x.receipt),integration:c.integration,archive:c.archive,testsStatus:o.testsStatus,humanReview:o.humanReview,minPassRate:o.minPassRate,minDelta:o.minDelta};}
function optionsFromState(state:FullEvalState):FullEvalOptions{return{...state.configuration,componentReceipts:[...state.configuration.componentReceipts],resume:true};}
export function planFullEval(options:FullEvalOptions):{context:Context;state:FullEvalState}{
 const c=context(options),fp=fingerprints(c,options);const jobs=PHASES.map((phase,i)=>{const id="full-eval/"+phase,depends_on=i?["full-eval/"+PHASES[i-1]]:[];return{id,phase,depends_on,input_hash:fp[phase],idempotency_key:hash({artifact:fp.validation,plan:fp.planning,scenario:phase,configuration:fp[phase],run_index:0}),status:"planned" as JobStatus,attempts:0,detail:"pending",output_hashes:{}};});
 return{context:c,state:{schema_version:"1.0",command:"full-eval",run_id:randomUUID(),graph_hash:hash(jobs.map(({id,depends_on,input_hash,idempotency_key})=>({id,depends_on,input_hash,idempotency_key}))),revision:0,status:"planned",configuration:persistedConfiguration(c,options),jobs}};
}
export function transitionJob(job:FullEvalJob,event:JobEvent,detail=job.detail):FullEvalJob{
 const allowed:Record<JobStatus,JobEvent[]>={planned:["start","cancel","skip"],running:["succeed","fail","block","cancel","skip"],succeeded:["succeed"],failed:["fail","retry"],blocked:["block","resume","cancel"],cancelled:["cancel","resume"],skipped:["skip","resume"]};
 if(!allowed[job.status].includes(event))throw new Error("invalid full-eval transition: "+job.status+" -> "+event);
 const target:Record<JobEvent,JobStatus>={start:"running",succeed:"succeeded",fail:"failed",block:"blocked",cancel:"cancelled",retry:"planned",resume:"planned",skip:"skipped"};
 const next=target[event];return{...job,status:next,attempts:event==="start"?job.attempts+1:job.attempts,detail,output_hashes:event==="start"||event==="retry"||event==="resume"?{}:job.output_hashes};
}
function loadState(path:string):FullEvalState|null{try{const value=JSON.parse(requireText(path));return value?.schema_version==="1.0"&&Array.isArray(value.jobs)&&value.configuration?value:null;}catch{return null;}}
function outputsCurrent(outputs:Record<string,string>|undefined):boolean{return outputs!==undefined&&Object.entries(outputs).every(([path,digest])=>fileHash(path)===digest);}
function mergeState(plan:FullEvalState,old:FullEvalState|null,resume:boolean):FullEvalState{
 if(!old||!resume)return plan;plan.run_id=old.run_id??old.graph_hash;let stale=false;const jobs=plan.jobs.map(job=>{const prior=old.jobs.find(x=>x.id===job.id);if(stale||!prior||prior.input_hash!==job.input_hash||(prior.status==="succeeded"&&!outputsCurrent(prior.output_hashes))){stale=true;return prior?{...job,attempts:prior.attempts}:job;}if(prior.status==="succeeded")return{...job,status:"succeeded" as JobStatus,attempts:prior.attempts,detail:prior.detail,output_hashes:prior.output_hashes??{}};if(prior.status==="failed")return transitionJob({...job,status:"failed",attempts:prior.attempts,detail:prior.detail},"retry","retry scheduled");if(prior.status==="blocked"||prior.status==="cancelled"||prior.status==="skipped")return transitionJob({...job,status:prior.status,attempts:prior.attempts,detail:prior.detail},"resume","resume scheduled");return job;});const verification=jobs.find(j=>j.phase==="verification")?.status==="succeeded"?old.verification:undefined;return{...plan,revision:old.revision,status:"planned",verification,jobs};
}
function checkpoint(path:string,state:FullEvalState,expectedRevision:number):void{
 mkdirSync(dirname(path),{recursive:true});const current=loadState(path);if(current&&current.revision!==expectedRevision)throw new Error("concurrent full-eval state revision changed: expected "+expectedRevision+", found "+current.revision);
 const temporary=path+".tmp-"+process.pid+"-"+Date.now()+"-"+Math.random().toString(16).slice(2);writeFileSync(temporary,JSON.stringify(state,null,2)+"\n",{flag:"wx"});try{renameSync(temporary,path);}finally{rmSync(temporary,{force:true});}
}
function processAlive(pid:number):boolean{try{process.kill(pid,0);return true;}catch(error:any){return error?.code==="EPERM";}}
function cleanupTemporaryStateFiles(directory:string):void{for(const name of readdirSync(directory)){if(name.startsWith(STATE_FILE+".tmp-"))rmSync(join(directory,name),{force:true});}}
function acquireLock(path:string):()=>void{
 mkdirSync(dirname(path),{recursive:true});for(let attempt=0;attempt<100;attempt++){const candidate=path+"."+process.pid+"."+Math.random().toString(16).slice(2);try{writeFileSync(candidate,JSON.stringify({pid:process.pid,created_at:Date.now()}),{flag:"wx"});linkSync(candidate,path);unlinkSync(candidate);return()=>{try{const value=JSON.parse(requireText(path));if(value.pid===process.pid)unlinkSync(path);}catch{}};}catch(error:any){rmSync(candidate,{force:true});if(error?.code!=="EEXIST")throw error;let stale=false;try{const value=JSON.parse(requireText(path)),age=Date.now()-statSync(path).mtimeMs;stale=!Number.isInteger(value.pid)||(!processAlive(value.pid)&&age>100);}catch{stale=true;}if(stale){try{unlinkSync(path);}catch{}cleanupTemporaryStateFiles(dirname(path));continue;}Atomics.wait(new Int32Array(new SharedArrayBuffer(4)),0,0,25);}}
 throw new Error("full-eval workspace is locked by another active run");
}
function legacyStatus(status:JobStatus):Phase["status"]{return status==="succeeded"?"pass":status==="failed"?"fail":status==="cancelled"||status==="skipped"?"skipped":status;}
function output(state:FullEvalState,c:Context,next_actions:string[],verification:unknown,o:FullEvalOptions){const status=state.status,exit_code=status==="success"?0:status==="blocked"||status==="cancelled"?3:status==="planned"?0:1;const phases:Phase[]=state.jobs.map(j=>({name:j.phase,status:legacyStatus(j.status),detail:j.detail}));return{schema_version:"1.0",command:"full-eval",status,exit_code,dry_run:Boolean(o.dryRun),resume:Boolean(o.resume),plugin:c.root,workspace:c.workspace,archive:c.archive,state_file:join(c.workspace,STATE_FILE),graph_hash:state.graph_hash,revision:state.revision,jobs:state.jobs,phases,components:c.components,next_actions,verification};}
export function fullEval(options:FullEvalOptions){
 const locator=context({pluginPath:options.pluginPath,workspace:options.workspace}),statePath=join(locator.workspace,STATE_FILE),lockPath=join(locator.workspace,LOCK_FILE);
 if(options.dryRun){const initial=planFullEval(options),state=mergeState(initial.state,loadState(statePath),Boolean(options.resume));return output(state,initial.context,[],state.verification??null,{...options,dryRun:true});}
 const release=acquireLock(lockPath);try{
  const existing=loadState(statePath);
  if(options.cancel){if(!existing)throw new Error("cannot cancel full-eval: no persisted graph exists in "+locator.workspace);const persistedOptions=optionsFromState(existing),c=context(persistedOptions),eventPath=join(c.workspace,EVENT_FILE),events=new ExecutionEventWriter(eventPath,existing.run_id??existing.graph_hash);const before=new Map(existing.jobs.map(job=>[job.id,job.status]));let state={...existing,jobs:existing.jobs.map(j=>j.status==="succeeded"||j.status==="cancelled"?j:{...j,status:"cancelled" as JobStatus,detail:"cancelled by request",output_hashes:{}}),status:"cancelled"};for(const job of state.jobs)if(before.get(job.id)!==job.status){events.append("job-transition",job.id,{phase:job.phase,status:job.status});events.append("phase-transition",job.id,{phase:job.phase,status:job.status});}events.append("cancellation",null,{status:"cancelled"});const expected=state.revision;state.revision++;checkpoint(statePath,state,expected);events.append("checkpoint",null,{revision:state.revision,status:state.status,state:protectedArtifactRef(statePath),jobs:state.jobs.map(({id,phase,status,attempts})=>({id,phase,status,attempts}))});return output(state,c,[],state.verification??null,options);}
  const effective=options.resume&&existing?{...optionsFromState(existing),...options,pluginPath:existing.configuration.pluginPath,workspace:existing.configuration.workspace,componentReceipts:options.componentReceipts??existing.configuration.componentReceipts}:options;
  const planned=planFullEval(effective),c=planned.context;let state=mergeState(planned.state,existing,Boolean(options.resume));if(existing&&!options.resume)state.revision=existing.revision;const next_actions:string[]=[];let verification:any=state.verification??null;
  const eventPath=join(c.workspace,EVENT_FILE),priorEvents=replayExecutionEvents(eventPath),runId=priorEvents.run_id??state.run_id,events=new ExecutionEventWriter(eventPath,runId);state.run_id=runId;
  if(!priorEvents.events.length)events.append("evaluation-created",null,{status:"planned",graph_hash:state.graph_hash,plugin:protectedArtifactRef(c.root),workspace:protectedArtifactRef(c.workspace)});
  else events.append("heartbeat",null,{status:state.status,resume:Boolean(options.resume)});
  const save=()=>{const expected=state.revision;state.revision++;checkpoint(statePath,state,expected);events.append("checkpoint",null,{revision:state.revision,status:state.status,state:protectedArtifactRef(statePath),jobs:state.jobs.map(({id,phase,status,attempts})=>({id,phase,status,attempts}))});};
  const run=(phase:string,fn:()=>{event:"succeed"|"fail"|"block"|"skip";detail:string;outputs?:string[]})=>{const index=state.jobs.findIndex(j=>j.phase===phase),job=state.jobs[index];if(job.status==="succeeded")return;if(job.attempts>0)events.append("retry",job.id,{phase,attempt:job.attempts+1});const unmet=job.depends_on.map(id=>state.jobs.find(j=>j.id===id)).filter(j=>j?.status!=="succeeded");if(unmet.length){state.jobs[index]={...job,status:"blocked",detail:"blocked by prerequisites: "+unmet.map(j=>j?.id+" ("+j?.status+")").join(", "),output_hashes:{}};events.append("job-transition",job.id,{phase,status:"blocked"});events.append("phase-transition",job.id,{phase,status:"blocked"});save();return;}state.jobs[index]=transitionJob(job,"start","running");events.append("job-transition",job.id,{phase,status:"running",attempt:state.jobs[index].attempts});events.append("phase-transition",job.id,{phase,status:"running"});save();const result=fn();state.jobs[index]=transitionJob(state.jobs[index],result.event,result.detail);if(result.event==="succeed")state.jobs[index].output_hashes=Object.fromEntries((result.outputs??[]).map(path=>[resolve(path),fileHash(resolve(path))]));events.append("job-transition",job.id,{phase,status:state.jobs[index].status,outputs:(result.outputs??[]).map(path=>protectedArtifactRef(path,fileHash(resolve(path))))});events.append("phase-transition",job.id,{phase,status:state.jobs[index].status});if(result.event==="fail")events.append("failure",job.id,{phase,status:"failed"});if(result.event==="block")events.append("approval-requested",job.id,{phase,status:"blocked",reason:"external evidence or human decision required"});save();};
  run("validation",()=>{const schema=validateAgentPluginSchema(c.root),structural=validate(c.root),ok=schema.valid&&!structural.errors.length;return{event:ok?"succeed":"fail",detail:ok?"validation passed":"validation failed"};});
  run("planning",()=>({event:"succeed",detail:c.skills.length+" bundled skill(s); deterministic graph "+state.graph_hash}));
  const missing=c.components.filter(x=>!x.available);for(const component of missing)next_actions.push(component.command+" # writes "+component.receipt);
  run("execution",()=>({event:missing.length?"block":"succeed",detail:missing.length?missing.length+" component receipt(s) missing":c.components.length+" component receipt(s) found",outputs:missing.length?[]:c.components.map(x=>x.receipt)}));
  const benchmark=join(c.integration,"benchmark.json");run("grading",()=>{if(!existsSync(benchmark)){next_actions.push("produce plugin integration outputs at "+q(benchmark)+" and "+q(join(c.integration,"review.html")));return{event:"block",detail:"benchmark missing: "+benchmark};}return{event:"succeed",detail:"benchmark evidence present",outputs:[benchmark]};});
  run("aggregation",()=>({event:options.testsStatus==="fail"?"fail":options.testsStatus==="pass"?"succeed":"block",detail:"test evidence: "+(options.testsStatus??"missing")}));
  const review=join(c.integration,"review.html");run("review",()=>{const ok=existsSync(review)&&options.humanReview==="pass";if(!ok)next_actions.push("review "+q(review)+" then rerun with --human-review pass --resume");return{event:ok?"succeed":"block",detail:!existsSync(review)?"review output missing":options.humanReview==="pass"?"human review complete":"human review pending",outputs:ok?[review]:[]};});
  run("improvement",()=>{mkdirSync(dirname(c.archive),{recursive:true});const packaged=packagePlugin(c.root,c.archive);return{event:packaged.status===0?"succeed":"fail",detail:packaged.status===0?c.archive:(packaged.stderr||packaged.stdout||"packaging failed").trim(),outputs:packaged.status===0?[c.archive]:[]};});
  run("verification",()=>{verification=verifyPlugin({pluginPath:c.root,profile:"release",componentReceipts:c.components.filter(x=>x.available).map(x=>x.receipt),integration:c.integration,archive:existsSync(c.archive)?c.archive:undefined,testsStatus:options.testsStatus,humanReview:options.humanReview,minPassRate:options.minPassRate,minDelta:options.minDelta});for(const [gate,value] of Object.entries(verification.gates) as [string,{status:Status;reason?:string}][])if(value.status==="blocked")next_actions.push("resolve "+gate+" gate: "+(value.reason??"evidence missing"));return{event:verification.status==="pass"?"succeed":verification.status==="fail"?"fail":"block",detail:"release verification: "+verification.status,outputs:[c.archive,benchmark,review]};});
  state.status=state.jobs.some(j=>j.status==="failed")?"failure":state.jobs.every(j=>j.status==="succeeded")?"success":"blocked";if(verification!==null)state.verification=verification;save();if(state.status==="success")events.append("completion",null,{status:"success",archive:protectedArtifactRef(c.archive,fileHash(c.archive))});else if(state.status==="failure")events.append("failure",null,{status:"failure"});return output(state,c,next_actions,verification,effective);
 }finally{release();}
}
