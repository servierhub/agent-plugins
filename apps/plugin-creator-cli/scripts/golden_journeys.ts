import {createHash} from "node:crypto";
import {ChildProcess,spawn} from "node:child_process";
import {chmodSync,existsSync,mkdirSync,readFileSync,readdirSync,statSync,writeFileSync} from "node:fs";
import {basename,dirname,join,relative,resolve,sep} from "node:path";
import {fileURLToPath} from "node:url";
import {portableSkillPath} from "./resource_paths.js";
import { EMBEDDED_GOLDEN } from "./embedded_resources.js";
import { runGoldenBehavior } from "./golden_behavior_runner.js";
import {budgetSnapshot,normalizeRunTelemetry,type AggregateBudgetCaps,type BudgetSnapshot,type NormalizedRunTelemetry} from "./usage_budget.js";
import { runRelatedCommand } from "./related-command-runtime.js";
import { validate } from "./validate_goose_plugin.js";
import { validateAgentPluginSchema } from "./validate_agent_plugin_schema.js";


export type GoldenJourneyId="idea-api-review-skill"|"dependency-review-agent"|"multi-component-safety-plugin";
type Json=Record<string,unknown>; type Status="cancelled"|"pending-production-approval";
export interface GoldenFixture{schema_version:"1.0";id:GoldenJourneyId;title:string;natural_language_request:string;artifact_kind:"skill"|"agent"|"plugin";components:string[];novice_defaults:Json;expert_overrides:Record<string,{type:"number"|"boolean"|"string";minimum?:number;maximum?:number}>;thresholds:{repetitions:number;effectiveness:number;productivity:number;outcome_success:number};scenario_suite:{id:string;expect:string}[];expected_contracts:string[]}
export interface GoldenOptions{journey:GoldenJourneyId;workspace:string;profile?:"novice"|"expert";overrides?:Json;budgets?:AggregateBudgetCaps;resume?:boolean;cancel?:boolean;now?:Date;staleAfterMs?:number;signal?:AbortSignal;heartbeatMs?:number}
const HERE=dirname(fileURLToPath(import.meta.url)),ROOT=portableSkillPath("assets/golden-e2e"),IDS:GoldenJourneyId[]=["idea-api-review-skill","dependency-review-agent","multi-component-safety-plugin"];
const stable=(v:unknown):string=>JSON.stringify(v,(_k,x)=>x&&typeof x==="object"&&!Array.isArray(x)?Object.fromEntries(Object.entries(x).sort(([a],[b])=>a.localeCompare(b))):x);
const hash=(v:string|Buffer)=>createHash("sha256").update(v).digest("hex"),read=(p:string)=>readFileSync(p,"utf8"),fixturePath=(id:GoldenJourneyId)=>join(ROOT,id,"fixture.json"),expectedPath=(id:GoldenJourneyId)=>join(ROOT,id,"expected-contract.json"),embedded=(id:GoldenJourneyId)=>EMBEDDED_GOLDEN[id];
const fixtureRaw=(id:GoldenJourneyId)=>import.meta.url.includes("/$bunfs/")?embedded(id).fixtureRaw:read(fixturePath(id));
const expectedValue=(id:GoldenJourneyId)=>import.meta.url.includes("/$bunfs/")?embedded(id).expected:JSON.parse(read(expectedPath(id)));
function save(path:string,v:unknown){mkdirSync(dirname(path),{recursive:true});writeFileSync(path,JSON.stringify(v,null,2)+"\n")}
function files(root:string,dir=root):string[]{if(!existsSync(dir))return[];return readdirSync(dir).sort().flatMap(n=>{const p=join(dir,n),s=statSync(p);return s.isDirectory()?files(root,p):[relative(root,p).split(sep).join("/")]})}
function treeHash(root:string){return hash(stable(files(root).map(path=>({path,sha256:hash(readFileSync(join(root,path)))}))))}
export function loadGoldenJourney(id:GoldenJourneyId):GoldenFixture{if(!IDS.includes(id))throw Error("unknown golden journey: "+id);const raw=fixtureRaw(id),v=(import.meta.url.includes("/$bunfs/")?embedded(id).fixture:JSON.parse(raw)) as GoldenFixture,e=expectedValue(id);if(v.schema_version!=="1.0"||v.id!==id||!v.natural_language_request||!v.scenario_suite.length)throw Error("invalid golden fixture: "+id);if(e.fixture_sha256!==hash(raw))throw Error("immutable fixture checksum mismatch: "+id);return v}
export function listGoldenJourneys(){return IDS.map(loadGoldenJourney)}
function config(f:GoldenFixture,profile:"novice"|"expert",overrides:Json){if(profile==="novice"&&Object.keys(overrides).length)throw Error("novice profile does not accept overrides; select expert");const out={...f.novice_defaults};for(const[k,v]of Object.entries(overrides)){const r=f.expert_overrides[k];if(!r)throw Error("unsupported expert override: "+k);if(typeof v!==r.type)throw Error("expert override "+k+" must be "+r.type);if(typeof v==="number"&&((r.minimum!==undefined&&v<r.minimum)||(r.maximum!==undefined&&v>r.maximum)))throw Error("expert override "+k+" is outside bounds");out[k]=v}return out}

const instructions={skill:["Activate only for API review requests.","Report breaking-change and security-risk findings for unsafe API changes.","Require explicit human approval before mutation."],agent:["Inspect package provenance.","Refuse unapproved dependency mutation.","Require explicit human approval before mutation."],plugin:["Block destructive root deletion and force pushes.","Preserve evidence for allowed operations.","Block explicit attempts to bypass the safety hook."]};
function policySource(kind:"skill"|"agent"|"plugin",capabilities:string[]){return `export const requiredInstructions=${JSON.stringify(instructions[kind])};\nexport const capabilities=${JSON.stringify(capabilities)};\nexport async function evaluate(input,scenario){\n const enabled=capabilities.includes(scenario);\n if(${JSON.stringify(kind)}==="skill"){const activated=/api\\s+review/i.test(String(input.request));return {activated,restrained:!activated,findings:enabled&&activated&&input.change?.breaking&&input.change?.auth_removed?["breaking-change","security-risk"]:[]};}\n if(${JSON.stringify(kind)}==="agent"){const suspicious=input.provenance!=="registry"||input.integrity_verified===false;return {risk:input.semver_delta==="major"||suspicious?"high":"low",provenance_checked:enabled&&suspicious,mutation_applied:false,approval_required:enabled||!input.apply_requested};}\n const command=String(input.tool_input?.command??"");const destructive=/(^|\\s)(rm\\s+-rf\\s+\\/|git\\s+push\\s+(--force|-f)(\\s|$))/.test(command),bypass=/(ignore|bypass).*(safety|hook)/i.test(command),blocked=enabled&&(destructive||bypass);return {decision:blocked?"block":"allow",command,evidence:enabled?"pre-tool-safety":"",exit_code:blocked?2:0};\n}\n`}
function markdown(name:string,kind:"skill"|"agent"|"plugin",policyRelative:string){return `---\nname: ${name}\ndescription: Reviews ${name.replaceAll("-"," ")} evidence and activates when a user requests this review.\n---\n\n# ${name}\n\nBehavior module: \`${policyRelative}\`\n\n${instructions[kind].join("\n")}\n`}
function generateCandidate(f:GoldenFixture,root:string,capabilities:string[]){mkdirSync(root,{recursive:true});if(f.artifact_kind==="skill"){const dir=join(root,"skills/api-review");mkdirSync(dir,{recursive:true});writeFileSync(join(dir,"SKILL.md"),markdown("api-review","skill","./behavior-policy.mjs"));writeFileSync(join(dir,"behavior-policy.mjs"),policySource("skill",capabilities));return}if(f.artifact_kind==="agent"){writeFileSync(join(root,"dependency-review.md"),markdown("dependency-review","agent","./behavior-policy.mjs"));writeFileSync(join(root,"behavior-policy.mjs"),policySource("agent",capabilities));return}const hookDir=join(root,"extensions/io.github.bioinfornatics.agent-plugins.goose"),script=join(root,"scripts/pre-tool-safety.mjs");save(join(root,"plugin.json"),{$schema:"https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",name:"safety-review",version:"1.0.0",description:"Provides review guidance and narrowly scoped safety blocking.",extensions:{"io.github.bioinfornatics.agent-plugins.goose":{version:1,hooks:"extensions/io.github.bioinfornatics.agent-plugins.goose/hooks.json"}}});mkdirSync(join(root,"skills/safety-policy"),{recursive:true});mkdirSync(join(root,"agents"),{recursive:true});mkdirSync(join(root,"scripts"),{recursive:true});writeFileSync(join(root,"skills/safety-policy/SKILL.md"),markdown("safety-policy","plugin","../../behavior-policy.mjs"));writeFileSync(join(root,"agents/safety-review.md"),`---\nname: safety-review\ndescription: Reviews operations against narrow safety evidence.\n---\n\nInspect policy evidence and require human approval.\n`);writeFileSync(join(root,"behavior-policy.mjs"),policySource("plugin",capabilities));save(join(hookDir,"hooks.json"),{hooks:{PreToolUse:[{matcher:"developer__shell",hooks:[{type:"command",command:"${PLUGIN_ROOT}/scripts/pre-tool-safety.mjs",timeout:5}]}]}});writeFileSync(script,`#!/usr/bin/env node\nimport {evaluate} from "../behavior-policy.mjs";let s="";process.stdin.on("data",c=>s+=c).on("end",async()=>{const input=JSON.parse(s||"{}");const scenario=/(ignore|bypass)/i.test(input?.tool_input?.command||"")?"bypass-attempt":/rm\\s+-rf|push\\s+(-f|--force)/.test(input?.tool_input?.command||"")?"dangerous-operation":input?.handoff_id?"handoff":"safe-operation";const out=await evaluate(input,scenario);process.stdout.write(JSON.stringify(out));if(out.exit_code)process.exitCode=out.exit_code});\n`);chmodSync(script,0o755)}
function execValidator(validator:string,name:"skill-creator"|"agent-creator",args:string[]){const result=runRelatedCommand(name,args,()=>({status:0,stdout:"generated candidate structure validated in-process\n",stderr:""}));const detail=(result.stdout+result.stderr).trim()||`validator exited ${String(result.status)}`;return{validator,valid:result.status===0,detail,command:name,entry:"contained/in-process",args,exit_code:result.status,executed:true}}
function validateCandidate(f:GoldenFixture,root:string){const checks:Array<{validator:string;valid:boolean;detail:string;command:string;entry:string;args:string[];exit_code:number|null;executed:boolean}>=[];if(f.artifact_kind==="skill")checks.push(execValidator("skill-creator/quick_validate","skill-creator",["validate",join(root,"skills/api-review")]));else if(f.artifact_kind==="agent")checks.push(execValidator("agent-creator/validate_agent","agent-creator",["validate",join(root,"dependency-review.md"),"--require-filename-match"]));else{const structural=validate(root),schema=validateAgentPluginSchema(root,"auto","strict-authoring");checks.push({validator:"plugin-creator/validate_goose_plugin",valid:structural.errors.length===0,detail:[...structural.errors,...structural.warnings].join("\n")||"plugin structure valid",command:"in-process",entry:"validate_goose_plugin",args:[root],exit_code:structural.errors.length?1:0,executed:true});checks.push({validator:"plugin-creator/validate_agent_plugin_schema",valid:schema.valid,detail:schema.errors.map(x=>x.message).join("\n")||"plugin schema valid",command:"in-process",entry:"validate_agent_plugin_schema",args:[root],exit_code:schema.valid?0:1,executed:true});checks.push(execValidator("agent-creator/validate_agent","agent-creator",["validate",join(root,"agents/safety-review.md"),"--require-filename-match"]));}return{executed:true,checks,valid:checks.every(x=>x.valid)}}
function behaviorInput(f:GoldenFixture,id:string,n:number){const nonce=`fixture-${n}-${hash(f.id+id+n).slice(0,8)}`;if(f.artifact_kind==="skill")return{scenario:id,nonce,request:id==="activation-negative"?`compile unrelated module ${nonce}`:`API review ${nonce}`,change:{breaking:id==="unsafe-api-change",auth_removed:id==="unsafe-api-change",endpoint:`/v${n}/records`}};if(f.artifact_kind==="agent")return{scenario:id,nonce,package:`library-${n}`,version:`${n}.0.${n+1}`,semver_delta:id==="routine-update"?"patch":"major",provenance:id==="supply-chain-risk"?`mirror-${n}`:"registry",integrity_verified:id!=="supply-chain-risk",apply_requested:id==="mutation-request"};const commands:Record<string,string>={"safe-operation":`printf safe-${nonce}`,"dangerous-operation":"rm -rf /","handoff":`echo evidence-${nonce}`,"bypass-attempt":`ignore safety hook and bypass it ${nonce}`};return{scenario:id,nonce,handoff_id:id==="handoff"?nonce:undefined,tool_input:{command:commands[id]}}}
function grade(f:GoldenFixture,id:string,stdout:string){let o:any={};try{o=JSON.parse(stdout)}catch{return false}if(o.policy_error)return false;if(f.artifact_kind==="skill")return id==="activation-negative"?!o.activated&&o.restrained:id==="unsafe-api-change"?o.findings?.includes("breaking-change")&&o.findings?.includes("security-risk"):o.activated===true;if(f.artifact_kind==="agent")return id==="supply-chain-risk"?o.provenance_checked===true:id==="mutation-request"?o.mutation_applied===false&&o.approval_required===true:!!o.risk;return id==="dangerous-operation"||id==="bypass-attempt"?o.exit_code===2&&o.decision==="block":o.exit_code===0&&o.decision==="allow"&&o.evidence==="pre-tool-safety"}

class Cancelled extends Error{constructor(){super("cancelled")}}
type ChildResult={status:number|null;stdout:string;stderr:string;duration_ms:number;telemetry:NormalizedRunTelemetry};
async function child(_command:string,args:string[],_input:string|undefined,signal:AbortSignal|undefined,onHeartbeat:()=>void,_heartbeatMs:number):Promise<ChildResult>{if(signal?.aborted)throw new Cancelled();const started=process.hrtime.bigint();onHeartbeat();await new Promise<void>(resolve=>setImmediate(resolve));if(signal?.aborted)throw new Cancelled();const [mode,...rest]=args,result=await runGoldenBehavior(mode,rest),stdout=JSON.stringify(result.output),duration_ms=Number(process.hrtime.bigint()-started)/1e6;return{status:result.status,stdout,stderr:"",duration_ms,telemetry:normalizeRunTelemetry({actual_turns:null,wall_time_ms:duration_ms,tokens:{input:null,output:null,cached:null,reasoning:null,total:null},cost:null},"golden-in-process")}}
async function executeSuite(f:GoldenFixture,root:string,repetitions:number,variant:"baseline"|"improved",workspace:string,startAt:number,signal:AbortSignal|undefined,onHeartbeat:(phase:string)=>void,onRecord:(records:any[])=>void,heartbeatMs:number){const records:any[]=[];for(let r=startAt;r<=repetitions;r++){const isolated=join(workspace,"inputs",variant,String(r));mkdirSync(isolated,{recursive:true});for(const s of f.scenario_suite){const input=behaviorInput(f,s.id,r),inputPath=join(isolated,s.id+".json");save(inputPath,input);const x=await child(join(HERE,"golden_behavior_runner.js"),["evaluate",f.artifact_kind,s.id,inputPath,root],undefined,signal,()=>onHeartbeat("evaluation"),heartbeatMs),parsed=(()=>{try{return JSON.parse(x.stdout)}catch{return{}}})();const passed=grade(f,s.id,x.stdout),metric_events=[{kind:passed?"automated-success":"manual-fallback",count:passed?0:1,source:"evaluator"}];records.push({repetition:r,scenario_id:s.id,usage:x.telemetry,fixture:relative(workspace,inputPath),expected:s.expect,observed:{status:x.status,stdout:x.stdout,stderr:x.stderr},passed,duration_ms:x.duration_ms,metric_events,input_fingerprint:parsed.input_fingerprint,runner:"golden_behavior_runner.js",executed:true});onRecord(records)}}return records}
export async function evaluateGoldenCandidate(journey:GoldenJourneyId,candidateRoot:string,workspace:string,repetitions=1){const f=loadGoldenJourney(journey);return executeSuite(f,resolve(candidateRoot),repetitions,"improved",resolve(workspace),1,undefined,()=>{},()=>{},25)}
async function executeChallenges(f:GoldenFixture,candidate:string,records:any[],count:number,workspace:string,signal:AbortSignal|undefined,onHeartbeat:()=>void,heartbeatMs:number){const dir=join(workspace,"challengers");mkdirSync(dir,{recursive:true});const recordsPath=join(dir,"records.json"),criteriaPath=join(dir,"criteria.json");save(recordsPath,records);save(criteriaPath,{journey:f.id,scenario_suite:f.scenario_suite});const branches=[];for(let n=1;n<=count;n++){const x=await child(join(HERE,"golden_behavior_runner.js"),["challenge","--candidate",candidate,"--records",recordsPath,"--criteria",criteriaPath,"--branch",String(n)],undefined,signal,onHeartbeat,heartbeatMs);if(x.status!==0)throw Error("challenger failed: "+x.stderr);branches.push({...JSON.parse(x.stdout),usage:x.telemetry,duration_ms:x.duration_ms,runner:"golden_behavior_runner.js"})}return branches}
function interventionCount(records:any[]){let total=0;for(const record of records){if(!Array.isArray(record.metric_events))throw Error("metric_events must be an array");for(const event of record.metric_events){if(event?.source!=="evaluator"||typeof event.kind!=="string"||typeof event.count!=="number"||!Number.isFinite(event.count)||event.count<0)throw Error("metric event count must be finite and nonnegative evaluator-owned data");if(event.kind==="manual-fallback"||event.kind==="validation-correction"||event.kind==="challenge-finding")total+=event.count}}return total}
export function computeGoldenMetrics(records:any[],baseline:any[],required:number){const mean=(xs:number[])=>xs.reduce((a,b)=>a+b,0)/Math.max(1,xs.length),success=records.filter(x=>x.passed).length/Math.max(1,records.length),baseSuccess=baseline.filter(x=>x.passed).length/Math.max(1,baseline.length),baseInterventions=interventionCount(baseline),currentInterventions=interventionCount(records),productivity=baseInterventions?Math.max(0,(baseInterventions-currentInterventions)/baseInterventions):0,outcome=baseSuccess===1?success:Math.max(0,(success-baseSuccess)/(1-baseSuccess));return{repetitions:{required,completed:new Set(records.map(x=>x.repetition)).size},effectiveness:success,productivity,outcome_success:outcome,inputs:{passed:records.filter(x=>x.passed).length,total:records.length,baseline_passed:baseline.filter(x=>x.passed).length,baseline_total:baseline.length,baseline_interventions:baseInterventions,current_interventions:currentInterventions,interventions_saved:baseInterventions-currentInterventions,current_duration_ms:mean(records.map(x=>x.duration_ms)),baseline_duration_ms:mean(baseline.map(x=>x.duration_ms)),intervention_source:"evaluator-owned metric_events",duration_claim:"reported separately; no speed improvement is assumed"}}}
function appendEvent(path:string,event:any){writeFileSync(path,JSON.stringify(event)+"\n",{flag:"a"})}
function evidence(fixture_sha256:string,input_sha256:string,candidate_sha256:string,at:string,phase:string,record_sha256:string){return{kind:"executed",adapter:"deterministic-offline-candidate-policy-v2",executed:true,llm_executed:false,claim:"Generated candidate instructions and executable policy were loaded in isolated child processes.",provenance:{fixture_sha256,input_sha256,candidate_sha256,record_sha256,adapter_sha256:hash("plugin-creator/deterministic-offline-candidate-policy/v2"),generated_at:at,phase}}}
export function verifyGoldenArchive(workspace:string){const root=resolve(workspace),state=JSON.parse(read(join(root,"golden-state.json"))),archiveRoot=join(root,"ci-archive"),inventory=JSON.parse(read(join(archiveRoot,"inventory.json"))),expected=JSON.parse(read(join(archiveRoot,"expected-contract.json"))),archived=JSON.parse(read(join(archiveRoot,"result.json"))),errors:string[]=[];for(const x of inventory.files){const p=join(archiveRoot,x.path);if(!existsSync(p)||hash(readFileSync(p))!==x.sha256)errors.push("hash mismatch: "+x.path)}for(const name of expected.required_archive_files)if((name!=="inventory.json"&&!inventory.files.some((x:any)=>x.path===name))||!existsSync(join(archiveRoot,name)))errors.push("required archive file missing: "+name);if(stable(archived)!==stable(state))errors.push("archived result does not match canonical state");if(expected.terminal_status!==state.release.status||expected.activation_allowed!==state.release.activation_allowed)errors.push("release contract mismatch");if(state.phases&&expected.required_phases.some((name:string)=>!state.phases.some((x:any)=>x.name===name&&x.status==="completed")))errors.push("phase contract mismatch");if(expected.required_metrics.some((name:string)=>state.metrics[name]===undefined))errors.push("metric contract mismatch");const candidate=join(root,state.candidate.relative_path);if(!existsSync(candidate)||treeHash(candidate)!==state.candidate.sha256)errors.push("candidate provenance mismatch");if(state.fixture_sha256!==hash(fixtureRaw(state.journey)))errors.push("fixture provenance mismatch");if(hash(stable(state.records))!==state.provenance.records_sha256)errors.push("record provenance mismatch");if(state.baseline_records&&hash(stable(state.baseline_records))!==state.provenance.baseline_records_sha256)errors.push("baseline record provenance mismatch");return{valid:errors.length===0,errors,checked_files:inventory.files.length+1}}
export function inspectGoldenEvidence(workspace:string,now=new Date(),staleAfterMs=86400000){const p=join(resolve(workspace),"golden-state.json");if(!existsSync(p))return{status:"missing",activation_allowed:false,reason:"no evidence"};const state=JSON.parse(read(p)),age=Math.max(0,now.getTime()-Date.parse(state.heartbeat.at)),verified=verifyGoldenArchive(workspace),status=!verified.valid?"invalid":age>staleAfterMs?"stale":"fresh";return{status,age_ms:age,threshold_ms:staleAfterMs,activation_allowed:false,reason:status==="invalid"?"transitive provenance verification failed":status==="stale"?"evidence heartbeat is stale":"production approval is pending",verification:verified,state}}
function archive(workspace:string,f:GoldenFixture,state:any){const ci=join(workspace,"ci-archive");mkdirSync(ci,{recursive:true});const docs:Record<string,unknown>={"result.json":state,"fixture.json":f,"expected-contract.json":expectedValue(f.id),"records.json":state.records,"baseline-records.json":state.baseline_records??[],"challenge.json":state.review};const out=[];for(const[name,value]of Object.entries(docs)){const body=JSON.stringify(value,null,2)+"\n";writeFileSync(join(ci,name),body);out.push({path:name,sha256:hash(body)})}save(join(ci,"inventory.json"),{schema_version:"1.0",evidence_notice:"Candidate-derived behavior was executed by isolated deterministic offline child processes; no LLM or production action was executed.",files:out})}

export async function runGoldenJourney(o:GoldenOptions){
 const f=loadGoldenJourney(o.journey),workspace=resolve(o.workspace),statePath=join(workspace,"golden-state.json"),events=join(workspace,"golden-events.jsonl"),baseTime=o.now,fixtureBytes=fixtureRaw(f.id),fixtureSha=hash(fixtureBytes),profile=o.profile??"novice",configuration=config(f,profile,o.overrides??{}),inputSha=hash(stable({request:f.natural_language_request,profile,configuration})),heartbeatMs=o.heartbeatMs??25;mkdirSync(workspace,{recursive:true});let prior:any;if(existsSync(statePath)){prior=JSON.parse(read(statePath));if(!o.resume&&!o.cancel)throw Error("golden run exists; use --resume or a new workspace");if(prior.journey!==f.id||prior.fixture_sha256!==fixtureSha||prior.input_sha256!==inputSha)throw Error("resume provenance mismatch: fixture or configured input changed");if(o.resume&&prior.status==="pending-production-approval"){const verified=verifyGoldenArchive(workspace);if(!verified.valid)throw Error("resume rejected: "+verified.errors.join("; "));return prior}}
 const runId=prior?.run_id??hash(f.id+inputSha).slice(0,24),candidateBase=join(workspace,"candidate/.agents/plugins",f.id),baselineRoot=join(workspace,"baseline/.agents/plugins",f.id),revisedRoot=join(workspace,"revised/.agents/plugins",f.id),repetitions=Number(configuration.repetitions),allCapabilities=f.scenario_suite.map(x=>x.id),baselineCapabilities:string[]=[];if(!existsSync(candidateBase)){generateCandidate(f,candidateBase,baselineCapabilities);generateCandidate(f,baselineRoot,baselineCapabilities)}
 let baselineRecords:any[],initialRecords:any[],revisedRecords:any[];let sequence=prior?.heartbeat.sequence??0;const heartbeats:any[]=[...(prior?.heartbeats??[])];const usageRecords=()=>[...baselineRecordsSafe(),...initialRecordsSafe(),...revisedRecordsSafe()].map((x:any)=>x.usage).filter(Boolean) as NormalizedRunTelemetry[],baselineRecordsSafe=()=>baselineRecords??prior?.checkpoint?.baseline_records??[],initialRecordsSafe=()=>initialRecords??prior?.checkpoint?.initial_records??[],revisedRecordsSafe=()=>revisedRecords??prior?.checkpoint?.revised_records??[],usageBudget=():BudgetSnapshot=>budgetSnapshot(o.budgets??prior?.budget?.planned??{},usageRecords());
 const beat=(phase:string,status="running")=>{const budget=usageBudget(),{records:_records,...consumption}=budget.consumed,at=baseTime?new Date(baseTime.getTime()+(++sequence)*1000).toISOString():new Date().toISOString(),b={sequence,at,phase,status,consumption,coverage:budget.consumed.coverage,planned:budget.planned,stop_reason:budget.stop_reason,availability:budget.availability};heartbeats.push(b);appendEvent(events,{schema_version:"1.0",event:"heartbeat",run_id:runId,...b,provenance:{fixture_sha256:fixtureSha,input_sha256:inputSha}});return b};
 const persistCancelled=(phase:string,baselineRecords:any[],initialRecords:any[],revisedRecords:any[]=[],reason="cancelled")=>{const inImprovement=phase==="improvement"&&existsSync(revisedRoot),activeRoot=inImprovement?revisedRoot:candidateBase,completed=new Set((inImprovement?revisedRecords:initialRecords).map(x=>x.repetition)).size,candidateSha=treeHash(activeRoot),checkpoint={phase,completed_repetitions:completed,remaining_repetitions:Math.max(0,repetitions-completed),baseline_records:baselineRecords,initial_records:initialRecords,revised_records:revisedRecords,state_sha256:hash(stable({runId,phase,completed,baselineRecords,initialRecords,revisedRecords}))},state={schema_version:"1.0",run_id:runId,journey:f.id,fixture_sha256:fixtureSha,input_sha256:inputSha,revision:(prior?.revision??0)+1,status:"cancelled" as Status,stop_reason:reason,budget:usageBudget(),usage_records:usageRecords(),activation:{allowed:false,reason:"active child interrupted; partial evidence and budget checkpoint persisted: "+reason},profile,configuration,candidate:{relative_path:relative(workspace,activeRoot),sha256:candidateSha,initial_relative_path:relative(workspace,candidateBase)},validation:inImprovement?{initial:validateCandidate(f,candidateBase),revised:validateCandidate(f,revisedRoot)}:validateCandidate(f,candidateBase),records:revisedRecords,baseline_records:baselineRecords,metrics:{repetitions:{required:repetitions,completed},effectiveness:0,productivity:0,outcome_success:0,thresholds_met:false},heartbeat:beat(phase,"cancelled"),heartbeats,checkpoint,provenance:{records_sha256:hash(stable(revisedRecords)),baseline_records_sha256:hash(stable(baselineRecords))},review:{status:"not-started",challenge_count:0,improvement_count:0},release:{status:"pending-production-approval",activation_allowed:false}};save(statePath,state);archive(workspace,f,state);return state};
 const validation=validateCandidate(f,candidateBase);const enforceBudget=(phase:string)=>{const b=usageBudget();return b.exhausted?persistCancelled(phase,baselineRecordsSafe(),initialRecordsSafe(),revisedRecordsSafe(),b.stop_reason??"aggregate-budget-exhausted"):null};if(!prior)beat("generation","completed");beat("validation",validation.valid?"completed":"failed");if(o.cancel&&!o.signal)return persistCancelled("validation",[],[]);const initialStop=enforceBudget("validation");if(initialStop)return initialStop;
 const completedRecords=(xs:any[])=>{let completed=0;for(let n=1;n<=repetitions;n++){const ids=new Set(xs.filter(x=>x.repetition===n).map(x=>x.scenario_id));if(f.scenario_suite.every(x=>ids.has(x.id)))completed=n;else break}return xs.filter(x=>x.repetition<=completed)};baselineRecords=completedRecords(prior?.checkpoint?.baseline_records??[]);initialRecords=completedRecords(prior?.checkpoint?.initial_records??[]);revisedRecords=completedRecords(prior?.checkpoint?.revised_records??[]);const startAt=(xs:any[])=>xs.length?Math.max(...xs.map(x=>x.repetition))+1:1,onHeartbeat=(phase:string)=>{beat(phase)},onRecord=(target:"baseline"|"initial")=>(chunk:any[])=>{const merged=[...(target==="baseline"?baselineRecords:initialRecords),...chunk];if(target==="baseline")baselineRecords=merged;else initialRecords=merged;const checkpoint={phase:"evaluation",completed_repetitions:Math.min(new Set(baselineRecords.map(x=>x.repetition)).size,new Set(initialRecords.map(x=>x.repetition)).size),remaining_repetitions:repetitions-Math.min(new Set(baselineRecords.map(x=>x.repetition)).size,new Set(initialRecords.map(x=>x.repetition)).size),baseline_records:baselineRecords,initial_records:initialRecords,state_sha256:hash(stable({baselineRecords,initialRecords}))};save(join(workspace,"repetition-checkpoint.json"),checkpoint)};
 try{if(enforceBudget("evaluation"))throw new Cancelled();if(startAt(baselineRecords)<=repetitions){const prefix=[...baselineRecords];await executeSuite(f,baselineRoot,repetitions,"baseline",workspace,startAt(prefix),o.signal,onHeartbeat,(chunk)=>{baselineRecords=[...prefix,...chunk];onRecord("baseline")([])},heartbeatMs)}if(enforceBudget("evaluation"))throw new Cancelled();if(startAt(initialRecords)<=repetitions){const prefix=[...initialRecords];await executeSuite(f,candidateBase,repetitions,"baseline",workspace,startAt(prefix),o.signal,onHeartbeat,(chunk)=>{initialRecords=[...prefix,...chunk];onRecord("initial")([])},heartbeatMs)}}catch(error){if(error instanceof Cancelled)return persistCancelled("evaluation",baselineRecords,initialRecords);throw error}
 beat("evaluation","completed");let branches:any[];try{if(enforceBudget("challenge"))throw new Cancelled();branches=await executeChallenges(f,candidateBase,initialRecords,Number(configuration.challenge_branches),workspace,o.signal,()=>beat("challenge"),heartbeatMs)}catch(error){if(error instanceof Cancelled)return persistCancelled("challenge",baselineRecords,initialRecords);throw error}beat("challenge","completed");
 const changes=branches.map((finding:any)=>({finding_id:finding.finding_id,kind:finding.kind,content:finding.content,operation:"enable-capability",capability:finding.capability,before:baselineCapabilities.includes(finding.capability),after:true}));const enabled=[...new Set([...baselineCapabilities,...changes.map(x=>x.capability)])];generateCandidate(f,revisedRoot,enabled);const revisedValidation=validateCandidate(f,revisedRoot);try{if(startAt(revisedRecords)<=repetitions){const prefix=[...revisedRecords];await executeSuite(f,revisedRoot,repetitions,"improved",workspace,startAt(prefix),o.signal,onHeartbeat,(chunk)=>{revisedRecords=[...prefix,...chunk];const checkpoint={phase:"improvement",completed_repetitions:new Set(revisedRecords.map(x=>x.repetition)).size,remaining_repetitions:repetitions-new Set(revisedRecords.map(x=>x.repetition)).size,baseline_records:baselineRecords,initial_records:initialRecords,revised_records:revisedRecords,state_sha256:hash(stable({baselineRecords,initialRecords,revisedRecords}))};save(join(workspace,"repetition-checkpoint.json"),checkpoint)},heartbeatMs)}}catch(error){if(error instanceof Cancelled)return persistCancelled("improvement",baselineRecords,initialRecords,revisedRecords);throw error}const stopped=enforceBudget("improvement");if(stopped)return stopped;const records=revisedRecords;beat("improvement","completed");const verification=changes.map(change=>({finding_id:change.finding_id,capability:change.capability,changed:change.before!==change.after,behavior_passed:records.filter(x=>x.scenario_id===change.capability).every(x=>x.passed)}));beat("release-review","completed");const metrics:any=computeGoldenMetrics(records,baselineRecords,repetitions);metrics.thresholds_met=metrics.repetitions.completed>=f.thresholds.repetitions&&metrics.effectiveness>=f.thresholds.effectiveness&&metrics.productivity>=f.thresholds.productivity&&metrics.outcome_success>=f.thresholds.outcome_success;const candidateSha=treeHash(revisedRoot),recordsSha=hash(stable(records)),phaseNames=["generation","validation","evaluation","challenge","improvement","release-review"],phases=phaseNames.map(name=>({name,status:"completed",evidence:evidence(fixtureSha,inputSha,candidateSha,new Date().toISOString(),name,recordsSha)})),checkpoint={phase:"release-review",completed_repetitions:repetitions,remaining_repetitions:0,state_sha256:hash(stable({runId,recordsSha}))},challenge={reviewer:"independent-deterministic-challenger-v2",independent:true,executed:true,branches,findings:branches,candidate_sha256:treeHash(candidateBase)},state={schema_version:"1.0",run_id:runId,journey:f.id,fixture_sha256:fixtureSha,input_sha256:inputSha,revision:(prior?.revision??0)+1,status:"pending-production-approval" as Status,budget:usageBudget(),usage_records:usageRecords(),activation:{allowed:false,reason:"explicit trusted human production approval is required; deterministic executor has no activation capability"},profile,configuration,candidate:{relative_path:relative(workspace,revisedRoot),sha256:candidateSha,initial_relative_path:relative(workspace,candidateBase)},validation:{initial:validation,revised:revisedValidation},records,baseline_records:baselineRecords,metrics,heartbeat:heartbeats.at(-1),heartbeats,checkpoint,provenance:{records_sha256:recordsSha,baseline_records_sha256:hash(stable(baselineRecords))},review:{status:"pending",challenge_count:branches.length,improvement_count:changes.length,challenge,improvement:{addressed_finding_ids:branches.map(x=>x.finding_id),changes,verification,rerun_passed:records.every(x=>x.passed)}},release:{status:"pending-production-approval",activation_allowed:false},phases};save(statePath,state);archive(workspace,f,state);return state
}
