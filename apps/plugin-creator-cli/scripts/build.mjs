import { spawnSync } from "node:child_process";import { fileURLToPath,pathToFileURL } from "node:url";import path from "node:path";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const tsc=path.join(root,"node_modules","typescript","bin","tsc");const result=spawnSync(process.execPath,[tsc,"-p",path.join(root,"tsconfig.json")],{cwd:root,stdio:"inherit"});if((result.status??1)!==0)process.exit(result.status??1);await import(pathToFileURL(path.join(root,"dist/scripts/decision_comprehension.js")).href);
