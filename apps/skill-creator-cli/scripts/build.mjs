import { cpSync, mkdirSync } from "node:fs";import { spawnSync } from "node:child_process";import { fileURLToPath } from "node:url";import path from "node:path";const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const tsc=path.join(root,"node_modules","typescript","bin","tsc");const result=spawnSync(process.execPath,[tsc,"-p",path.join(root,"tsconfig.json")],{stdio:"inherit"});if(result.status!==0)process.exit(result.status??1);mkdirSync(path.join(root,"dist","eval-viewer"),{recursive:true});cpSync(path.join(root,"eval-viewer","viewer.html"),path.join(root,"dist","eval-viewer","viewer.html"));